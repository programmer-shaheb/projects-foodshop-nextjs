import React from "react";
import { ToastContainer } from "react-toastify";

const ToastNotification = () => {
  return <ToastContainer position="top-center" autoClose={3000} closeOnClick />;
};

export default ToastNotification;
